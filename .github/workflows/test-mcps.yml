name: Run MCP tests

on:
  workflow_dispatch:  # Manual trigger only - redundant with quality_control.yml

jobs:
  test-mcps:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        mcp:
          - name: "adios"
            path: "iowarp_mcp_servers/adios"
          - name: "arxiv"
            path: "iowarp_mcp_servers/arxiv"
          - name: "compression"
            path: "iowarp_mcp_servers/compression"
          - name: "darshan"
            path: "iowarp_mcp_servers/darshan"
          - name: "hdf5"
            path: "iowarp_mcp_servers/hdf5"
          - name: "jarvis"
            path: "iowarp_mcp_servers/jarvis"
          - name: "lmod"
            path: "iowarp_mcp_servers/lmod"
          - name: "node-hardware"
            path: "iowarp_mcp_servers/node-hardware"
          - name: "pandas"
            path: "iowarp_mcp_servers/pandas"
          - name: "parallel-sort"
            path: "iowarp_mcp_servers/parallel-sort"
          - name: "parquet"
            path: "iowarp_mcp_servers/parquet"
          - name: "plot"
            path: "iowarp_mcp_servers/plot"
          - name: "slurm"
            path: "iowarp_mcp_servers/slurm"

    name: Test ${{ matrix.mcp.name }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        
    - name: Install uv
      uses: astral-sh/setup-uv@v3
      
    - name: Install Slurm dependencies
      if: matrix.mcp.name == 'slurm'
      run: |
        sudo apt-get update
        sudo apt-get install -y slurm-wlm

    - name: Run tests for ${{ matrix.mcp.name }}
      working-directory: ${{ matrix.mcp.path }}
      run: |
        uv run pytest --tb=short -v

